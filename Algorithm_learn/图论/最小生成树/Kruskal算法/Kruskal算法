Kruskal算法核心：
    1. 边排序， 遍历边
    2. 如果边的两个端点存在不属于生成树的节点， 则将该节点加入树；否则pass, 遍历下一条边


复杂度分析：
            时间:
                O(m*log(m));
            空间:
                O(m+n); m 是指边， n是指并查集所需的整数（可以理解成节点）
